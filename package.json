{
  "name": "cinco",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "All": "npx playwright test --reporter=line,allure-playwright",
    "smoke": "npx playwright test --grep @Smoke",
    "regression": "npx playwright test --grep @Regression",
    "API": "npx playwright test --grep @API",
    "regressionChromeOnly": "npx playwright test --project =Chrome execution",
    "regressionIphone12Webkit": "npx playwright test --config=playwright.config1.js",
    "clean-test-results-Slack": "rimraf test-results.json",
    "testSlack": "npm run clean-test-results-Slack && npx playwright test --reporter=json > test-results.json || echo Test run failed but continuing...",
    "posttest": "node send-to-slack.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.14.0",
    "allure-playwright": "^3.2.1",
    "npm-run-all": "^4.1.5",
    "playwright-slack-report": "^1.1.89",
    "rimraf": "^6.0.1"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "module": "^1.0.0",
    "nodemailer": "^6.10.1",
    "playwright": "^1.52.0"
  }
}
